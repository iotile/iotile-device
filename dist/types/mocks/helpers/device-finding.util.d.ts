/**
 * Helper functions for finding specific mock devices to test with
 */
import { IOTileAdvertisement } from "../../iotile-device";
export declare function findByDeviceID(id: number, devices: IOTileAdvertisement[]): IOTileAdvertisement | null | undefined;
