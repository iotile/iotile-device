/**
 * Helper functions for finding specific mock devices to test with
 */
import { IOTileAdvertisement } from "../../src";
export declare function findByDeviceID(id: number, devices: IOTileAdvertisement[]): any;
